<div class="sub-nav">
  {{#link-to 'services.new' classNames="btn btn--primary"}}
    <i class="ss-write btn__icon"></i>New service{{/link-to}}
</div>

<div class="cards--one-up">
{{#each service in model}}
  <div class='card'>
    <div class="card__header">
      <h3 class="card__header-title">
        {{#link-to 'service' service.id}}
        {{service.id}}
        {{/link-to}}
      </h3>
    </div>
    {{#link-to 'service' service.id}}
    <div class="card__body">
        <p>{{service.description}}</p>
    </div>
    {{/link-to}}
  </div>
{{/each}}
</div>
